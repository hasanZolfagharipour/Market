<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.zolfagharipour.market.viewModel.CategoryViewModel" />

        <variable
            name="position"
            type="Integer" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

    <TextView
        android:id="@+id/textViewItemRowCategoryLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginStart="@dimen/_8mdp"
        android:fontFamily="@font/font_yekan"
        android:textColor="@color/black"
        android:text="@{viewModel.categoryProductList.get(position).name}"
        android:layout_marginTop="@dimen/_20mdp"
        android:textStyle="bold"
        android:textSize="16sp"/>


    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewCategory"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/_8mdp"
        app:layout_constraintTop_toBottomOf="@id/textViewItemRowCategoryLabel"/>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>